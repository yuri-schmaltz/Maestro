"""Local Director skill plugins.

Each subdirectory can define a skill_manifest.json file and a Python module that
contains a planner class inheriting BasePlanner. The registry automatically
discovers these manifest files and registers the planner at runtime.
"""
