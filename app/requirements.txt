--extra-index-url https://aiinfra.pkgs.visualstudio.com/PublicPackages/_packaging/ort-cuda-13-nightly/pypi/simple/
# All versions below are pinned to the set that was tested against the
# Maestro release. If you need to upgrade, do it one package at a time and
# re-test Studio + Director end-to-end before committing. Do NOT loosen
# pins without testing — many of these have known breakage on newer releases
# (HuggingFace tokenizers/accelerate, torch/torchcodec/xformers ABI, etc).
# 3.7.12 preserves custom native linear forwards while applying LoRAs.
# LTX-2.5 INT8 ConvRot checkpoints require that path; older MMGP releases
# produce corrupted/noise video whenever an otherwise-compatible LoRA is active.
mmgp==3.7.12

# Core AI stack
diffusers==0.36.0
transformers==4.57.1   # 4.57.x adds transformers.models.qwen3_vl (required by HiDream O1)
tokenizers==0.22.1     # transformers 4.57.1 requires tokenizers>=0.22.0,<=0.23.0

accelerate==1.12.0
tqdm==4.67.3
imageio==2.37.2
imageio-ffmpeg==0.6.0
einops==0.8.2
sentencepiece==0.2.1
open_clip_torch==3.2.0
numpy==2.1.2
num2words==0.5.14

# Video & media
moviepy==1.0.3
av==16.1.0
ffmpeg-python
pygame==2.6.1
sounddevice==0.5.5
soundfile==0.13.1
mutagen==1.47.0
pyloudnorm==0.2.0
librosa==0.11.0
faster-whisper==1.2.1
openai-whisper==20250625  # Required by Scenema audio for Whisper-medium alignment (separate namespace from faster-whisper)
speechbrain==1.0.3
audio-separator==0.36.1
pyannote.audio==3.3.2
torchcodec==0.10.0

# UI & interaction
gradio==5.29.0
pywebpush==2.3.0  # Background Web Push; VAPID keys/subscriptions stay local to Maestro
dashscope==1.25.12
loguru
s3tokenizer
conformer==0.3.2
spacy_pkuseg
spacy
gradio_rangeslider==0.0.8

# Vision & segmentation
opencv-python==4.13.0.92
segment-anything==1.0
rembg[gpu]==2.0.65
onnxruntime-gpu==1.22.0; python_version < "3.11"
onnxruntime-gpu==1.25.0.dev20260210001; python_version >= "3.11"
decord==0.6.0
timm==1.0.24
# insightface: prebuilt Windows wheels hosted on a deepbeepmeep GitHub
# release mirror (PyPI only ships sdist, which fails to build on Windows
# without MSVC build tools). If the mirror ever disappears, users will
# need to install Visual Studio Build Tools and install from sdist.
insightface @ https://github.com/deepbeepmeep/insightface/releases/download/insightface/insightface-0.7.3-cp310-cp310-win_amd64.whl ; sys_platform == "win32" and python_version == "3.10"
insightface @ https://github.com/deepbeepmeep/insightface/releases/download/insightface/insightface-0.7.3-cp311-cp311-win_amd64.whl ; sys_platform == "win32" and python_version == "3.11"
insightface @ https://github.com/deepbeepmeep/insightface/releases/download/insightface/insightface-0.7.3-cp312-cp312-win_amd64.whl ; sys_platform == "win32" and python_version == "3.12"
insightface==0.7.3 ; sys_platform == "linux"
facexlib==0.3.0
taichi==1.7.4
vector_quantize_pytorch==1.27.19


# chumpy wheel hosted on GitHub to avoid sdist build isolation issue
chumpy @ https://github.com/deepbeepmeep/chumpy/releases/download/v0.71/chumpy-0.71-py3-none-any.whl
smplfitter @ https://github.com/deepbeepmeep/smplfitter/releases/download/v0.2.10/smplfitter-0.2.10-py3-none-any.whl

# Config & orchestration
omegaconf==2.3.0
hydra-core==1.3.2
easydict==1.13
pydantic==2.10.6

# Math & modeling
torchdiffeq==0.2.5
tensordict==0.11.0
peft==0.17.0
vector-quantize-pytorch==1.27.19
matplotlib==3.10.8
gguf==0.17.1
flash-linear-attention==0.4.1

# Utilities
ftfy==6.3.1
piexif==1.1.3
nvidia-ml-py==13.590.48
misaki==0.9.4
phonemizer-fork  # G2P backend used by misaki.espeak — namespace `phonemizer` (Scenema + multitalk kokoro path)
espeakng-loader  # Bundles the espeak-ng binary so phonemizer doesn't need a separate system install
pydub            # Imported by seedvc/app_vc.py — used by Scenema's voice-reference (SeedVC) mode
gitdb==4.0.12
gitpython==3.1.45
stringzilla==4.0.14
xxhash==3.6.0
munch==4.0.0
wetext==0.1.2
# Director planner — recovers from common LLM JSON output mistakes
# (missing commas, trailing commas, single quotes, unclosed brackets).
# Used as a last-resort fallback in BasePlanner._parse_json_response;
# the planner still works without it but loses the ability to recover
# from these very common Gemma / Qwen JSON output bugs.
json_repair==0.59.5

# Script/roteiro attachment — extracts plain text from attached .pdf
# story files so the Director can plan a short film directly from it.
# .txt/.md scripts are read natively without any dependency.
pypdf==6.18.1

# Optional / commented out
# transformers==4.46.3  # for llamallava pre-patch
# rembg==2.0.65         # non-GPU fallback
# huggingface_hub[hf_xet]  # slows down everything
iopath>=0.1.10
pycocotools
